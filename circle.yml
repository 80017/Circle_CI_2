machine:
  node:
    version: 7.10.1
  environment:
    DATABASE_URL: localhost:28015/rethinkdb_data


dependencies:
 pre:
   - npm install
   - npm install chai@latest -S

test:
 override:
    - npm run test
