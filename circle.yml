machine:
  node:
    version: 7.10.1
 
deployment:
 production:
  branch: master
  command:
   - ./deploy_dev.sh


dependencies:
 pre:
   - npm install
   - npm install chai@latest -S

test:
 override:
    - npm run test
