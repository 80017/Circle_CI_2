
version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/node:7.10

dependencies:
 pre:
   - npm install
   - npm install chai@latest -S

test:
 override:
    - npm run test
